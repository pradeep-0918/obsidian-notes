It Supports Windows, linux , mac
Standalone environment
Linux -
64 bits
32 bits
Configration for master slave
After Spliting nodes
each nodes consist of each own their OS System -> Windows / Linux (preferred)/ Mac
5 nodes -> linux Ecosystem -> each node installed additionally Hadoop -> After installing choose one node as Main(Master Node) -> other are (Slave Node)......
Master node must be extra ram usage compare to other slave node

