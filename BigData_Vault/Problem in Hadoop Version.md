It Supports Windows, Linux , mac...

Standalone environment
Linux -
64 bits
32 bits

Configuration for master slave
After Splitting nodes
each nodes consist of each own their OS System -> Windows / Linux (preferred)/ Mac
5 nodes -> Linux Ecosystem -> each node installed additionally Hadoop -> After installing choose one node as Main(Master Node) -> other are (Slave Node)......
Master node must be extra ram usage compare to other slave node.....

Each node consist of Ext file System and HDFS file System
-> Ext Standalone file System over the head HDFS is Connected with each other...
-> It Splits the large file in small small chunks 
-> Cluster part of the Machine 
-> real time Scenario read/write operation can be made in edge node (real time)

1 GB -> HDFS  WRITE OPERATION -> upload in Ext file System -> using Command -> API 